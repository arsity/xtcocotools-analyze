[build-system]
requires = ["setuptools>=64", "wheel", "numpy>=1.23", "cython>=0.29"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
